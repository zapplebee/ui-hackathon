/* istanbul ignore file */
/* tslint:disable */
/* eslint-disable */

/**
 * StepSecret is the pipeline representation of a secret
 * from a secrets block for a step in a pipeline.
 */
export type PipelineStepSecret = {
  source?: string;
  target?: string;
};
